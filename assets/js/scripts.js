$(document).ready(function() {
	
	// $('.main-content').scroll3D();

	// $('.s3d__navigation').remove();

	$(".project").hover3d({
		selector: ".project__card",
		// shine: true,
		sensitivity: 4,
	});
	
});